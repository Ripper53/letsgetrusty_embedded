# Embedded Runner
This is an example project for `embedded_tester`.

## memory.x files
The `memory.x` files are for QEMU simulation. Read the `run_tests.sh` file at the root of this repository to see how it's used.
