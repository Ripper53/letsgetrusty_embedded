version: "3.8"

services:
  embedded-runner:
    build: .
    volumes:
      - .:/usr/src/app
    working_dir: /usr/src/app
    environment:
      - RUSTFLAGS=-Awarnings
    stdin_open: true
    tty: true
